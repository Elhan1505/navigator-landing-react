# 🎨 Универсальное руководство по медиафайлам (Vite + React фреймворк)

Это руководство применимо к **любому** лендингу, созданному на базе этого фреймворка.

---

## 📐 Архитектура фреймворка

### Принцип разделения контента и кода

```
ваш-лендинг/
├── public/              ← ВСЕ медиафайлы (картинки, видео, иконки)
├── src/
│   ├── data/
│   │   └── content.ts  ← Весь текстовый контент + ПУТИ к медиа
│   ├── sections/       ← Компоненты секций (код)
│   └── components/     ← Переиспользуемые компоненты
```

**Идея:**
- **Контент** (тексты, пути к картинкам) → `content.ts`
- **Медиа** (сами файлы) → `public/`
- **Код** (как отображать) → `sections/` и `components/`

---

## 🗂️ Структура папки `public/`

### Рекомендуемая организация (универсальная):

```
public/
├── og-image.jpg           ← Open Graph для соцсетей (обязательно)
├── favicon.svg            ← Иконка сайта (обязательно)
│
├── images/                ← Все изображения
│   ├── hero/
│   │   ├── hero-bg.jpg           ← Фон первого экрана
│   │   └── hero-illustration.svg ← Иллюстрация для Hero
│   │
│   ├── sections/
│   │   ├── problem.svg           ← Картинка для секции "Проблема"
│   │   ├── solution.svg          ← Картинка для секции "Решение"
│   │   ├── process.svg           ← Картинка для секции "Процесс"
│   │   └── results.png           ← Картинка для секции "Результаты"
│   │
│   ├── screenshots/
│   │   ├── screenshot-1.jpg
│   │   ├── screenshot-2.jpg
│   │   └── screenshot-3.jpg
│   │
│   └── logos/
│       ├── logo.svg              ← Основной логотип
│       └── logo-white.svg        ← Белая версия логотипа
│
├── videos/                ← Все видео
│   ├── hero-bg.mp4               ← Фоновое видео для Hero
│   ├── demo.mp4                  ← Демонстрационное видео
│   └── testimonial.mp4           ← Видео отзыв
│
└── icons/                 ← Кастомные иконки (опционально)
    ├── feature-1.svg
    ├── feature-2.svg
    └── feature-3.svg
```

---

## 📝 Как добавлять пути к медиа в `content.ts`

### Базовый принцип

В файле `src/data/content.ts` для каждой секции можно добавить поля с путями к медиа:

```typescript
export const content = {
  // ===== 1. МЕТА-ДАННЫЕ (обязательно) =====
  meta: {
    title: "Название вашего лендинга",
    description: "Описание для SEO",
    ogImage: "/og-image.jpg",        // ← Путь к картинке для соцсетей
    favicon: "/favicon.svg",         // ← Путь к иконке (опционально)
    url: "https://your-site.com"
  },

  // ===== 2. HERO СЕКЦИЯ (первый экран) =====
  hero: {
    // Текстовый контент
    title: "Заголовок",
    subtitle: "Подзаголовок",
    description: "Описание",

    // Медиа
    backgroundImage: "/images/hero/hero-bg.jpg",        // Фоновая картинка
    backgroundVideo: "/videos/hero-bg.mp4",             // Или фоновое видео
    illustration: "/images/hero/hero-illustration.svg", // Иллюстрация
    logo: "/images/logos/logo.svg",                     // Логотип

    // CTA
    cta: {
      text: "Кнопка",
      href: "#section"
    }
  },

  // ===== 3. СЕКЦИЯ "ПРОБЛЕМА" =====
  problem: {
    title: "Заголовок секции",
    description: "Описание",

    // Медиа
    illustration: "/images/sections/problem.svg",  // Иллюстрация проблемы
    video: "/videos/problem-explain.mp4",          // Объясняющее видео (опционально)

    // Список проблем
    items: [
      { text: "Проблема 1", icon: "/icons/problem-1.svg" },
      { text: "Проблема 2", icon: "/icons/problem-2.svg" }
    ]
  },

  // ===== 4. СЕКЦИЯ "РЕШЕНИЕ" =====
  solution: {
    title: "Заголовок",
    description: "Описание",

    // Медиа
    illustration: "/images/sections/solution.svg",
    screenshot: "/images/screenshots/product-ui.jpg",

    features: [
      { text: "Фича 1", icon: "/icons/feature-1.svg" },
      { text: "Фича 2", icon: "/icons/feature-2.svg" }
    ]
  },

  // ===== 5. СЕКЦИЯ "КАК ЭТО РАБОТАЕТ" (процесс) =====
  process: {
    title: "Как это работает",

    // Видео-демо (рекомендуется)
    demoVideo: "/videos/demo.mp4",

    steps: [
      {
        id: 1,
        title: "Шаг 1",
        description: "Описание",
        image: "/images/process/step-1.svg",  // Картинка для каждого шага
        icon: "Heart" // Или иконка из Lucide (текущий вариант)
      },
      {
        id: 2,
        title: "Шаг 2",
        description: "Описание",
        image: "/images/process/step-2.svg"
      }
    ]
  },

  // ===== 6. СЕКЦИЯ "РЕЗУЛЬТАТЫ" =====
  results: {
    title: "Что вы получите",

    // Скриншоты результатов
    screenshots: [
      "/images/screenshots/result-1.jpg",
      "/images/screenshots/result-2.jpg",
      "/images/screenshots/result-3.jpg"
    ],

    // Или видео с результатами
    resultVideo: "/videos/results-showcase.mp4"
  },

  // ===== 7. СЕКЦИЯ "ОТЗЫВЫ" =====
  testimonials: {
    title: "Отзывы",

    items: [
      {
        name: "Иван Иванов",
        text: "Отзыв",
        avatar: "/images/testimonials/avatar-1.jpg",  // Аватар
        video: "/videos/testimonial-1.mp4"            // Видео-отзыв (опционально)
      }
    ]
  },

  // ===== 8. FOOTER =====
  footer: {
    logo: "/images/logos/logo-white.svg",  // Белая версия логотипа
    copyright: "© 2025 Компания"
  }
}
```

---

## 🎯 Соглашение об именовании файлов

### Универсальные правила:

#### 1. **Обязательные файлы** (всегда одинаковые названия):
```
/og-image.jpg          ← Для соцсетей (1200×630px)
/favicon.svg           ← Иконка сайта (32×32px)
```

#### 2. **Hero секция**:
```
/images/hero/hero-bg.jpg              ← Фон
/images/hero/hero-illustration.svg    ← Основная иллюстрация
/videos/hero-bg.mp4                   ← Фоновое видео
```

#### 3. **Секции лендинга** (используйте понятные названия):
```
/images/sections/{название-секции}.svg

Примеры:
/images/sections/problem.svg
/images/sections/solution.svg
/images/sections/how-it-works.svg
/images/sections/features.svg
/images/sections/pricing.svg
```

#### 4. **Скриншоты/примеры**:
```
/images/screenshots/{описание}-{номер}.jpg

Примеры:
/images/screenshots/dashboard-1.jpg
/images/screenshots/mobile-app-1.jpg
/images/screenshots/result-example-1.jpg
```

#### 5. **Видео**:
```
/videos/{назначение}.mp4

Примеры:
/videos/demo.mp4              ← Основное демо
/videos/hero-bg.mp4           ← Фон для Hero
/videos/testimonial-1.mp4     ← Видео-отзыв
/videos/tutorial.mp4          ← Обучающее видео
```

#### 6. **Логотипы**:
```
/images/logos/logo.svg              ← Основной
/images/logos/logo-white.svg        ← Белая версия
/images/logos/logo-icon.svg         ← Только иконка (без текста)
```

---

## 🔧 Как использовать пути в компонентах

### Пример 1: Добавить фоновое изображение в Hero

**Шаг 1:** Добавьте путь в `content.ts`:
```typescript
hero: {
  title: "Заголовок",
  backgroundImage: "/images/hero/hero-bg.jpg",  // ← Добавили
}
```

**Шаг 2:** Используйте в компоненте `HeroSection.tsx`:
```tsx
export const HeroSection = () => {
  const { hero } = content;

  return (
    <Section
      style={{
        backgroundImage: `url(${hero.backgroundImage})`,
        backgroundSize: 'cover',
        backgroundPosition: 'center'
      }}
    >
      <h1>{hero.title}</h1>
    </Section>
  );
};
```

### Пример 2: Добавить иллюстрацию в секцию

**Шаг 1:** В `content.ts`:
```typescript
problem: {
  title: "Проблема",
  illustration: "/images/sections/problem.svg",  // ← Добавили
}
```

**Шаг 2:** В `ProblemSection.tsx`:
```tsx
export const ProblemSection = () => {
  const { problem } = content;

  return (
    <Section>
      <div className="grid md:grid-cols-2 gap-8">
        <div>
          <h2>{problem.title}</h2>
        </div>
        <div>
          <img
            src={problem.illustration}
            alt={problem.title}
            className="w-full h-auto"
          />
        </div>
      </div>
    </Section>
  );
};
```

### Пример 3: Добавить видео

**Шаг 1:** В `content.ts`:
```typescript
process: {
  title: "Как это работает",
  demoVideo: "/videos/demo.mp4",  // ← Добавили
}
```

**Шаг 2:** В `ProcessSection.tsx`:
```tsx
export const ProcessSection = () => {
  const { process } = content;

  return (
    <Section>
      <h2>{process.title}</h2>
      <video controls className="w-full max-w-3xl mx-auto">
        <source src={process.demoVideo} type="video/mp4" />
      </video>
    </Section>
  );
};
```

---

## 📋 Чек-лист для нового лендинга

Когда создаете новый лендинг на этом фреймворке:

### 1. Подготовка медиа
- [ ] **Обязательно:** `og-image.jpg` (1200×630px)
- [ ] **Обязательно:** `favicon.svg` (32×32px)
- [ ] **Рекомендуется:** Фон для Hero (`hero-bg.jpg` или `hero-bg.mp4`)
- [ ] **Рекомендуется:** Иллюстрации для секций
- [ ] **Опционально:** Демо-видео, скриншоты

### 2. Размещение файлов
- [ ] Положите файлы в `public/`
- [ ] Используйте структуру папок: `images/`, `videos/`, `icons/`
- [ ] Следуйте соглашению об именовании

### 3. Обновление content.ts
- [ ] Добавьте пути к медиа в соответствующие секции
- [ ] Используйте абсолютные пути (начинаются с `/`)
- [ ] Проверьте, что пути соответствуют реальным файлам

### 4. Обновление компонентов (если нужно)
- [ ] Добавьте `<img>` или `<video>` теги в компоненты секций
- [ ] Используйте пути из `content.ts`
- [ ] Добавьте `alt` для изображений

### 5. Тестирование
- [ ] Запустите `npm run dev`
- [ ] Проверьте, что все изображения загружаются
- [ ] Проверьте на мобильных размерах

---

## 🎨 Рекомендации по медиа

### Размеры изображений

| Тип | Размер | Формат |
|-----|--------|--------|
| Open Graph | 1200×630px | JPG/PNG |
| Favicon | 32×32px или 64×64px | SVG/PNG |
| Hero фон | 1920×1080px (Full HD) | JPG/WebP |
| Иллюстрации | 800×600px или векторные | SVG/PNG |
| Скриншоты | 1200×800px | JPG/PNG |
| Логотип | Векторный | SVG |
| Аватары | 200×200px | JPG/PNG |

### Форматы

- **Иконки, логотипы, иллюстрации:** SVG (масштабируется без потери качества)
- **Фотографии, скриншоты:** JPG или WebP (сжатый)
- **Прозрачность:** PNG
- **Видео:** MP4 (H.264, сжатый для веба)

### Оптимизация

- **Изображения:** Сжимайте через [TinyPNG](https://tinypng.com) или [Squoosh](https://squoosh.app)
- **Видео:** Не более 5-10 МБ, используйте онлайн-компрессоры
- **Размер страницы:** Стремитесь к < 2 МБ общего веса

---

## 🚀 Примеры для разных типов лендингов

### Лендинг SaaS продукта
```typescript
{
  hero: {
    backgroundImage: "/images/hero/hero-bg.jpg",
    logo: "/images/logos/logo.svg",
    screenshot: "/images/screenshots/dashboard.jpg"
  },
  features: {
    items: [
      { icon: "/icons/analytics.svg", ... },
      { icon: "/icons/security.svg", ... }
    ]
  }
}
```

### Лендинг курса/обучения
```typescript
{
  hero: {
    instructorPhoto: "/images/instructor.jpg",
    backgroundVideo: "/videos/course-preview.mp4"
  },
  curriculum: {
    modules: [
      { thumbnail: "/images/modules/module-1.jpg", ... }
    ]
  }
}
```

### Лендинг мобильного приложения
```typescript
{
  hero: {
    appIcon: "/images/app-icon.png",
    mockup: "/images/phone-mockup.png"
  },
  screenshots: {
    images: [
      "/images/screenshots/screen-1.jpg",
      "/images/screenshots/screen-2.jpg"
    ]
  }
}
```

---

## ❓ FAQ

**Q: Почему пути начинаются с `/`?**
A: В Vite файлы из `public/` доступны по корневому пути. `/og-image.jpg` = `public/og-image.jpg`

**Q: Можно ли использовать изображения из `src/assets/`?**
A: Можно, но лучше использовать `public/` для простоты. Файлы из `src/assets/` требуют импорта.

**Q: Как добавить изображение из CDN или внешнего URL?**
A: Просто укажите полный URL: `"https://example.com/image.jpg"`

**Q: Нужно ли обновлять `vite.config.ts`?**
A: Нет, `base` уже настроен для GitHub Pages. Пути к медиа работают автоматически.

---

## 📚 Итого

1. **Медиа → `public/`** (с понятной структурой папок)
2. **Пути → `content.ts`** (абсолютные, начинаются с `/`)
3. **Использование → компонентах** (`<img src={content.hero.image} />`)
4. **Именование → согласно соглашению** (понятные, последовательные названия)

Эта система работает для **любого** лендинга на этом фреймворке!
