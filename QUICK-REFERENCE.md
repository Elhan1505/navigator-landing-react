# ⚡ Быстрая шпаргалка по медиа (Vite + React)

## 📁 Куда класть файлы

```
public/
├── og-image.jpg          ← Обязательно (1200×630)
├── favicon.svg           ← Обязательно (32×32)
├── images/
│   ├── hero/
│   ├── sections/
│   ├── screenshots/
│   └── logos/
└── videos/
```

## 📝 Структура content.ts

```typescript
export const content = {
  meta: {
    ogImage: "/og-image.jpg",
    favicon: "/favicon.svg"
  },

  hero: {
    // Текст
    title: "...",
    description: "...",

    // Медиа (добавьте по необходимости)
    backgroundImage: "/images/hero/hero-bg.jpg",
    backgroundVideo: "/videos/hero-bg.mp4",
    illustration: "/images/hero/illustration.svg",
    logo: "/images/logos/logo.svg"
  },

  [название_секции]: {
    // Текст
    title: "...",

    // Медиа (добавьте по необходимости)
    image: "/images/sections/[название].svg",
    video: "/videos/[название].mp4",
    screenshot: "/images/screenshots/[название].jpg"
  }
}
```

## 🎯 Соглашение об именовании

| Тип | Путь | Пример |
|-----|------|--------|
| Open Graph | `/og-image.jpg` | Фиксированное название |
| Favicon | `/favicon.svg` | Фиксированное название |
| Hero фон | `/images/hero/hero-bg.jpg` | Фиксированное название |
| Иллюстрация секции | `/images/sections/[имя].svg` | `/images/sections/problem.svg` |
| Скриншот | `/images/screenshots/[имя]-[№].jpg` | `/images/screenshots/result-1.jpg` |
| Видео | `/videos/[назначение].mp4` | `/videos/demo.mp4` |
| Логотип | `/images/logos/logo.svg` | Основной логотип |

## 🔧 Как использовать в компонентах

```tsx
// 1. Импортируйте content
import { content } from '@/data/content';

// 2. Извлеките нужную секцию
const { hero } = content;

// 3. Используйте в JSX
<img src={hero.illustration} alt={hero.title} />

// Или фоновое изображение
<div style={{ backgroundImage: `url(${hero.backgroundImage})` }}>

// Или видео
<video src={hero.backgroundVideo} autoPlay muted loop />
```

## ✅ Чек-лист для нового лендинга

- [ ] Положите `og-image.jpg` в `public/`
- [ ] Положите `favicon.svg` в `public/`
- [ ] Создайте структуру папок в `public/` (`images/`, `videos/`)
- [ ] Добавьте пути к медиа в `content.ts`
- [ ] Обновите компоненты для отображения медиа
- [ ] Проверьте локально: `npm run dev`

## 📐 Размеры файлов

- **og-image.jpg**: 1200×630px
- **favicon.svg**: 32×32px
- **Hero фон**: 1920×1080px
- **Иллюстрации**: 800×600px или SVG
- **Видео**: Не более 5-10 МБ

## 🚀 Все готово!

1. Файлы → `public/`
2. Пути → `content.ts`
3. Использование → компонентах

**Полное руководство:** См. `FRAMEWORK-MEDIA-GUIDE.md`
